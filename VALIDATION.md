# 验证记录

## 1.0 发布决定与小范围复核

发布前通过现有追问入口，以真实DeepSeek对两份相反的自编样本进行定向事实复核：orders.js的“任何调用者取消都会中止共享任务”被纠正为只有创建任务时使用的signal影响共享请求；shared-wait.js的独立等待取消不影响共享请求、Promise.all失败不停止其他请求也正确指出。总尝试3次与最多额外重试2次得到区分。

这只是两次定向复核，不证明稳定性，未引入自动二次调用或改变产品链路。项目所有者接受复杂并发解释不稳定等已知限制，决定发布1.0源码版本；下方历史记录中的“发布阻塞”反映当时验收判断，现作为公开已知问题跟进，不抹去失败记录。发布范围和未验证项目见[v1.0.0说明](docs/releases/v1.0.0.md)。

## 复杂代码解释约束与选择器间距（待通过真实模型验收）

修改共用事实规则和零基础表达规则：核对尝试次数、错误路径、取消信号的作用范围、查找键、累计与舍入顺序，限制无关性能结论；点读优先说明当前数据的作用。没有修改解析器、源码位置、模型输出结构、请求控制、收藏或模式存储。解释风格选择器仍是原生select，视觉箭头距右侧12px并预留文字间距，高对比模式恢复原生箭头。

最终修订版226项自动测试、19个公开样本通过。浏览器实际确认选择器间距和原有导入、分析、模式入口；使用交互工具，未运行历史Playwright脚本或桌面安装器。自动测试通过不代表生成内容准确。

真实DeepSeek deepseek-flash复验：订单流程标题改为“最多尝试3次”；Python讲解保留库存不足不扣减、先累计后舍入及复合排序，未再自动添加错误的线性复杂度结论。最终并发行说明能够简短说明等待各项成功/失败并保留原顺序。

**仍未通过：** 同一共享取消追问首轮能区分首个请求信号，后续重复却再次声称“谁先取消谁就会让共享任务中止”，与源码不符；生成解释仍有术语较密和遗漏边界的情况。因此本轮不能宣称事实错误已稳定修复，不应据此发布1.0。规则是约束而不是语义验证器；不加入输出替换或按样本名称硬编码来掩盖错误。

新增三份自编静态验收材料及正反场景基准，见[复杂源码人工回归](docs/AI_COMPLEX_ACCEPTANCE.md)。第三份shared-wait.js实际复验能区分两次请求、外层等待取消不取消共享网络请求、Promise.all失败不自动停止其余请求；未据此推定所有错误路径均覆盖。不执行样本，不在默认测试中调用付费模型。继续保留草稿状态，需要进一步解决事实可靠性并做真实模型复验。

## 表达与标题调整

零基础规则采用自然的成人交流表达，避免“交回去、存住”等含糊替代词，补充 try、catch、console.log、error、await、return 的上下文示例及反例边界。文档区两行标题合并为“代码概览”，专门主题的原有标题保留。顶部显示所配置的模型名称，例如“已接入 DEEPSEEK-flash”；这是配置显示，不表示额外进行了联网验证。

表达与标题调整后，226 项测试和 19 个公开样例通过；模型名称显示随后通过语法检查及浏览器实际验证。此轮没有新增付费模型调用，最新措辞规则尚未做真实 AI 复验。

## 零基础友好 / 标准模式

226 项自动测试及 19 个公开样例通过；补充静态脚本路由断言后单独复跑 API 与模式测试通过。检查五个 AI 解释入口的两种模式传递、未知模式回退、原文保留、模式切换取消请求并清除旧解释、偏好存储失败及缓存重置。浏览器已验证默认零基础友好、切换后的本地说明、标准偏好刷新保留、知识卡细节折叠及易错点保留。既有收藏内容不随模式改写。

本地资料复用已有通俗说明并折叠详细内容，不宣称已人工重写全部语言知识目录。真实 DeepSeek deepseek-flash 完成两种模式逐行解释对照、return 点读、未知 getUser 的 await 解释及零基础讲解稿。本轮共 6 次请求；未声称未知函数会访问网络。发现旧点读提问强制举例和“下一步”，已改为按模式分别提问，并新增实际请求构造测试。通过追问入口提交收紧后的同义提问，真实回复为“等 getUser() 做完，把交回的结果记在 user 里；这里的 await 让这行先等结果回来，再往下走。”最终点读提问修改需页面刷新后加载，未读取或转移密钥来强制刷新原配置页。

自动测试不等同于模型质量验收；未运行旧版 Playwright 脚本。生成结果仍需对照源码，短句要求不得省略重要条件或猜测外部函数行为；真实回答仍存在简要模式偶尔补充多余语句的可能，不承诺每次严格两句。

## 2026-09-24 验收问题修复

工作台请求统一区分主动取消、超时和服务错误；取消后的晚到回复不再作为成功结果进入缓存。总览、流程、逐行解释、词语知识卡和讲解稿共享异步语义约束；识别、修复与 OCR 不附加解释规则。

222 项自动测试及 19 个公开样例通过；追加的 API 集成断言单独复跑通过，确认五个解释入口均携带规则，识别、修复与 OCR 没有携带。新增取消、晚到回复、重试、超时与服务错误区分测试。未运行历史 Playwright 脚本，本次浏览器操作通过交互工具验证。

真实 DeepSeek `deepseek-flash` 在修复版本验证：停止生成显示“已停止生成，源码未修改，可以重试。”；再次请求成功。自编组合样本覆盖无 await 的 async、then/catch 消费、抛错、await 拒绝后 try/catch 恢复，以及普通同步函数。总览、逐行解释和简要讲解稿正确区分上述行为；讲解稿明确 async 返回 Promise 与是否包含 await 无关，且调用方也可使用 then/catch。此轮 4 次 UI 请求（含 1 次取消），不代表所有模型或任意输入均正确。其他解释入口已完成规则传递集成测试，未逐一重复真实调用。

持续抽检用例见 [异步解释人工回归](docs/AI_ASYNC_ACCEPTANCE.md)。Python 反例、真实 AI 完整 lesson 收藏闭环、安装包与真人试用仍待验收；未发布 1.0。

## 粘贴 JavaScript 的语言识别修复

Java/TypeScript/JavaScript 的关键字证据不再读取注释和字符串；Java 的 package 证据要求声明形式，ES 模块导出不再误走 Shell 检测。导入为 .txt 的 JavaScript/TypeScript 使用内部脚本扩展名供编译器解析，不改变用户原文件名或源码。

新增自编正反例覆盖含 package 文本的 JS 类、ES 导出、Java 声明、TS 类型和 .txt 导入。212 项测试与 19 份公开样本通过；浏览器对用户提供的代码仅作静态分析，确认显示 JavaScript 与函数目录。用户原始输入未加入仓库测试样本。

## 按需格式修复

工具栏移除重复示例和常驻清理按钮。输入检测到可疑复制格式，或本地解析报告语法问题时，显示修复入口。本地清理与可选 AI 建议均先预览差异，点击应用后替换输入，可撤销；用户继续编辑后撤销入口失效，避免覆盖后续修改。

验证：210 项自动测试、19 份公开样本通过。新增 AI 修复返回结构、空值、容量限制、鉴权和模拟服务 API 检查。浏览器手动验证正常输入隐藏提示、代码围栏提示、本地预览与应用/撤销、缺失缩进提示、模拟 AI 建议应用/撤销及停止生成。修复弹窗已移除两段通用说明小字。未调用真实模型，未重新验证 OCR 或桌面安装器。

## 0.8.0 发布验证

本次发布合并现有工作台与 README，统一应用版本号。实际验证结果与限制见 [0.8.0 发布说明](docs/releases/v0.8.0.md)。后续章节包含历史记录，不能把旧结果直接当作当前版本的新验收。

## 0.7.0 预览版验收范围（历史）

## 可以复现的检查

1. `pnpm test`：自编教学例子的预期结果；重试正反例、标准库别名与同名覆盖、源文位置和本文件关联。
2. `pnpm run test:release`：固定提交的公开代码样本，核对哈希、许可证、语言识别、结构范围与部分行为。
3. 服务启动后，`pnpm run test:browser`：真实页面导入，用途摘要跳转、源码高亮、知识卡收藏及刷新保留、390px 布局；需要 Playwright 和 Edge。

页面检查可以用 `WHO_PLAYWRIGHT_MODULE` 指定已安装的 Playwright 路径，用 `CODELINGO_URL` 指定本地服务地址。检查报告写入 `.browser-artifacts`，不应提交。

Flask JSON provider 和 node-jsonfile 是在规则写完后取得的新样本，未按它们的变量名或函数名编写规则。其它公开样本保留跨语言回归。

分析结果附版本、构建编号、原始输入 SHA-256。输入不同、构建不同，不能直接当作同一次测试比较。源码引用保留原文位置，恢复后的分析不代表原始程序已经能运行。

## 不应误解为已完成的验证

- 公开代码只被静态读取，不表示其依赖已安装或整个上游项目能运行。
- 教学卡中的自编 Python / JavaScript 例子在测试中核对实际结果；用户导入的代码不会执行。
- 没有真实新手参与的理解测试，不宣称比通用 AI 更快或更准确。
- 没有完整审计所有依赖，也没有验证所有 AI 服务、Windows 多屏截图和 OCR 组合。
- 稳定读出结构不等于理解全部业务。跨文件继承、动态修改、框架调用、复杂异步调度仍存在缺口。

## 公开包与本地反馈样本

开发工作区保留用户反馈与更广泛的历史回归；待公开包不含用户上传的私人源码、原始聊天粘贴、运行日志或个人路径。公开包使用自编等价场景及带许可证的上游样本验收。因此公开包与本地完整测试的数量不同，不能混为一谈。

下一轮用户验证：给初学者一段未见过的代码，观察其是否能说明用途、输入输出及失败路径，并指出对应源码。记录误解和用时，不以知识卡数量替代理解效果。

## 依赖检查

本版升级 YAML、PostCSS 与 Lodash 相关依赖，版本固定在 package.json 和 pnpm-lock.yaml。CSS 解析显式关闭 source map 加载，回归测试确认导入 CSS 的映射注释不会读取本地映射文件。
可用 `pnpm audit --prod` 复查已知依赖通告；数据库会变化，零条通告不等于没有安全问题。

## 0.7.1 本地增量验收

130 项自动测试、19 份既有公开样本以及 9 次页面导入通过。新增 tests/pedagogy.test.js 和 tests/browser-pedagogy.cjs；后者默认使用自编例子和公开样本，WHO_PRIVATE_INITIALIZER 可选指定历史反馈对照文件。截图和结果来自实际页面，未进行独立新手理解实验。本次未重新验证安装或更新公开压缩包。

## 逐行阅读回归

`tests/line-reading.test.js` 纳入 `pnpm test`。`node tests/browser-line-reading.cjs` 自行启动临时服务，通过 Edge 验证选行、完整语句、旧视图切换、AI 请求状态和手机布局；AI 回复使用模拟接口。依赖与运行方法见 [逐行阅读说明](docs/LINE_READING.md#验证)。

## AI 调用回归

收藏分类增量：使用 pnpm 10.15.1 运行 218 项自动测试及 19 份公开样本，全部通过。新增知识卡校验、定义/不完整响应不提供收藏、分类与旧收藏兼容、源码关联去重回归。浏览器在本机模拟 AI 下实际验证完整知识卡收藏、名称释义不显示收藏按钮、分类筛选、关键词搜索、手动改分类、刷新保留及重复收藏去重；此轮未运行旧版独立浏览器脚本，未验证真实模型分类质量或桌面安装器。

识别辅助增量：216 项自动测试与 19 份公开样本通过。`tests/ai-language.test.js` 和 API 回归覆盖语言识别、本地解析校验、低置信度、无效响应、失败与取消。页面使用本机模拟模型验证未勾选 AI 总览时仍可辅助识别，以及取消后保留源码；未验证真实模型识别质量。识别结果只用于选择本地解析器，不直接生成结构节点。

完整讲解稿：`tests/ai-talk.test.js` 检查输出完整性、参数和导出；`tests/api.test.js` 通过本机模拟模型验证 `/api/talk`。`node tests/browser-talk.cjs`（设置 `WHO_PLAYWRIGHT_MODULE` 和 `CODELINGO_URL`）验证生成、阅读基础/详略、导出、失败保留上稿、设置失效、取消晚到响应、重试和手机布局；回复是模拟数据，不能据此声称真实模型稿件质量通过。该轮全量测试 207 项和 19 个公开样本通过。真实模型稿件尚未验证；此前提供的凭据最后一次调用返回 401。可用有效配置运行显式选择的 `tests/browser-ai-live.cjs`，该脚本现已包含完整讲解稿生成。

`tests/ai-client.test.js` 和 `tests/api.test.js` 检查接口参数、源码保留、选区原文、超时和错误分类。`tests/browser-ai-states.cjs` 验证本地结果优先、取消和失败恢复。真实调用脚本 `tests/browser-ai-live.cjs` 为显式选择的额外检查，不纳入默认测试，运行方法见逐行阅读说明。

## 三列 AI 工作台验收

`tests/ai-flow.test.js` 验证分支、源码范围、顶层与嵌套调用、名称覆盖和 UTF-16 词语位置；`tests/api.test.js` 通过模拟模型服务验证 `/api/flow` 完整请求链路。启动服务后，设置 `CODELINGO_URL`、`CODELINGO_PYTHON` 和 `WHO_PLAYWRIGHT_MODULE`，运行 `node tests/browser-studio.cjs`，验证嵌套展开、递归终止、缓存、过期响应、词语弹窗与 390px 布局。该脚本使用模拟 AI 回复。新流程接口的真实 DeepSeek 测试收到 401，未完成新功能的实际模型质量验收。

## 可选云端账户（本地验证）

237 项自动测试与 19 个公开样本通过。新增模拟服务与存储/UI 测试覆盖身份核验、账户隔离、未登录收藏不自动上传、过期/退出、并发保存、版本冲突、断网与损坏数据保留。浏览器确认账户弹窗、云服务未配置提示和登录按钮禁用。没有创建云项目；真实 SMTP、两邮箱登录、跨设备同步、SQL 权限隔离脚本尚未运行。当前环境没有 PostgreSQL/Supabase CLI。不能将模拟通过视为生产云端验收完成。

## GitHub 登录与入口精简（本轮）

邮箱登录替换为 GitHub OAuth；新增 PKCE 绑定、单次回调、取消/过期、错误来源和回调敏感参数不反射测试。移除左侧“代码／讲解室”文字和输入区两个按钮，未将按钮搬至设置；拖拽导入及悬浮窗截图实现保留。真实 GitHub/Supabase 登录、数据库权限脚本仍待配置云项目后验证。

本轮 238 项自动测试、19 个公开样本通过。浏览器确认 GitHub 登录按钮在未配置时禁用、左侧文字与两个输入按钮移除、设置中无替代按钮。原生 Windows 悬浮窗未重新执行验收，截图相关脚本/API 保持不变。

账户窗口不再显示初始配置横幅，登录按钮居中；侧栏四项使用一致高度与图标/文字行高。浏览器截图检查通过，账户 UI 定向测试通过；未配置云端时按钮仍禁用，悬停提示原因。
