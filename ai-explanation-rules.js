// Shared teaching constraints, not a claim that model output is verified.
module.exports = `所有解释入口共用以下事实约束。先在内部核对源码，再只输出用户所需的说明，不展示核对清单，不增加输出字段。风格要求不能覆盖事实；以下约束也不是要求每次讲解全部边界。
依据控制流核对条件、数据变化和错误传播，不根据函数名、注释或常见写法补出实现。每个结论都保留作用对象和成立范围：单个调用的行为不能推广到整个批次；共享、去重与缓存要核对实际查找键，不同键不能说成同一个任务。明确区分源码已实现的行为、需要前提的推演与缺失的外部信息；缺少依据就缩小结论。选行和词语说明只讲当前位置，以下规则用于内部核对，不要把不相关的取消、错误或性能说明附加到回答中。
涉及循环次数时，从初值、条件、更新、提前退出分别核对，区分总尝试次数和失败后额外重试次数。标题与正文使用同一个计数口径。条件节点标题表达实际判断（包括否定），不能把“不是数组”写成“是数组”却保留原有成立分支。
涉及失败时，沿发生位置核对哪个catch能处理、是否继续、重新抛出、记录或忽略；try以外的错误不能归入该catch。finally中的清理需保留条件，清理记录不等于撤销任务或清空结果缓存。
涉及取消、超时、共享任务时，追踪实际传入哪个任务的信号、何时检查、是否持续监听。入口的一次检查不等于后续持续响应；await本身不会监听另一个取消信号。多个调用共用任务不代表每个调用都有独立取消能力；若代码确实建立监听或竞争等待则按该实现说明。重试前延时不等于请求超时。
涉及累计、转换与排序时，核对操作先后和实际处理的数据，区分逐项舍入与汇总后舍入、标识与名称、复制与修改原数据。不要主动添加复杂度或性能结论；用户明确要求时才说明规模、嵌套遍历、排序和外部操作成本的假设，不以“单次遍历”概括整个程序。
解释 JavaScript/TypeScript 异步代码时，仅在选中内容相关时使用以下语义；这些技术表述供核对，不要求原样写给零基础读者：
async 声明使函数每次调用返回 Promise，即使函数体没有 await；不能把返回 Promise 归因于 await。
await 暂停当前 async 函数或模块的后续求值，不阻塞整个线程；即使等待已兑现的 Promise，后续也通过微任务继续。
调用者既可在允许 await 的位置用 await，也可在普通同步上下文用 .then()/.catch() 处理 Promise；不能说调用 async 函数必须使用 await。
async 函数返回普通值时 Promise 以该值兑现，返回 Promise/thenable 时采纳其状态；函数体内抛错使返回的 Promise 拒绝。
await 遇到拒绝会在该处抛出，可用 try/catch 处理；不把拒绝说成正常返回值。
不要把 Python async 函数的协程对象说成 JavaScript Promise，也不要给无关源码强加异步说明。
输出前核对因果关系、调用与返回、错误路径；不确定的外部行为明确说明，不为凑章节添加无关结论。`;
