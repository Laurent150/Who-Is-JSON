> 历史记录：其中的反馈输入已在公开迁移时替换为自编回归样本，旧文字和数字不代表对原始反馈文件的新验收。迁移范围见 [迁移说明](docs/PUBLIC_MIGRATION.md)。

# Who Is JSON 0.6.0 验收记录

日期：2026-09-12。定位：本地解释优先，AI 为可选扩展。

## 针对两轮反馈的调整

1. 初学者说明采用用途、过程、源码、按需语法学习的层级。Bash 常见判断用通俗问题表达，变量名独立显示，并说明选择后接着做什么。
2. JSON 从顶层字段复述改为递归配置树。配置层级不冒充执行顺序，命令字符串不再混入长段落；实际文字与保留转义的源文分别展示。
3. 有限识别 npm 与 Claude Code 字段约定，展示许可与触发的用途、超时单位、提示文字的边界。未识别字段、外部脚本和未展开层级列为缺口。
4. 新增 8 个 JSON 基础主题，共 82 个教学主题；继续支持收藏与导出。没有知识识别记录时显示“知识覆盖尚未评估”。主题数量不等于完整语言或源码覆盖。
5. 移除旧 JSON/npm 解释分支，把配置用途、Bash 通俗解释及用途模式从语法读取与界面中分离，采用统一 guide 模型。详见 ARCHITECTURE.md；未宣称重写整个历史项目。

## 本轮实际验证

- 整套自动测试 104/104 通过，包含原有 14 个真实开源样本的解析、源文范围和损坏片段回归。
- 最后提取 Bash 用途模块并调整其文案后，相关 12 项测试再次通过。这 12 项属于前述测试集合，不重复累计为 116 项。
- 新增回归覆盖：截图配置的用途/转义/原文范围、未知 schema、未知字段、PreToolUse 与 PostToolUse 的不同、args/async 对相关说明的影响、基本 JSON 值、Unicode、巨大层级、特殊字段名，以及 Bash 改名后的判断含义。
- 配置截图重建样本在浏览器导入两次，检查两个分组、实际文字、精确高亮、JSON 知识、外部脚本缺口、收藏刷新后保留、390px 无横向溢出。
- 用户 Bash 原文整理版与带复制转义版各导入两次，检查入口/函数、循环、判断、高亮、内嵌 JavaScript 边界、旧文件名清理与窄屏布局。
- 真实文件 is-number.js、binary-search.py、java-Factorial.java、vue-ci.yml、typescript-package.json 各做两次浏览器导入与节点检查。两组浏览器脚本均未记录页面异常。
- 手动查看实际截图：JSON 命令原文/实际文字、JSON matcher 窄屏学习页、Bash 条件和后续步骤窄屏页。发现并修正了新例子的转义错误、旧 npm 嵌套说明遗漏及若干专业术语。
- 自编 JavaScript/Python 教学例子执行并核对结果；JSON 例子读取并核对结果；Java/Shell 例子仅做语法核对。没有执行用户脚本、配置命令或外部检查脚本。

## 样本来源与复现

tests/fixtures/user-settings.json 按本次用户截图重建，非原文件的逐字校验版本；主要字段、许可列表和 hook 结构保留。tests/fixtures/user-credentials.sh 与 user-credentials-copied.txt 来自前次用户粘贴的脚本。真实开源样本沿用项目已有归档，来源、提交/修订、哈希与许可见 tests/corpus/manifest.json。本轮没有声称新下载这些仓库或运行其完整项目。

运行：node --test tests/*.test.js。启动服务后运行 tests/browser-guides.cjs 与 tests/browser-shell.cjs，需要 Playwright + Edge，可通过 WHO_PLAYWRIGHT_MODULE 指定已安装模块。浏览器检查采用独立上下文，不读取或更改用户收藏。源码包含测试脚本与样本，不包含依赖目录和临时浏览器数据。

配置约定核实来源：[Claude Code 设置](https://code.claude.com/docs/en/settings)、[权限](https://code.claude.com/docs/en/permissions)、[Hooks](https://code.claude.com/docs/en/hooks)。用途解释属于有限规则，不能代替目标软件对配置的实际加载、权限检查和执行。

## 仍有边界

- JSON 树支持通用数据结构，应用语义只覆盖当前实现的字段。识别为 Claude Code 是字段组合推断；未知字段不猜测。
- 配置中的 hook_lint.sh 未自动读取，不能确认检查内容或通过情况。内嵌语言、外部命令、动态展开及复杂调用仍需上下文。
- Bash 的复杂方言、case、管道、heredoc、while/until 等未完整展开。有限业务模式不等于通用算法理解。
- 自动测试与页面检查不能证明零基础用户一定理解；后续应让用户复述用途、判断不同输入的路径、指出想修改的位置，做实际可用性验证。
- 未重新全面验证系统截图、多屏 OCR、全部历史浏览器文案断言或外部 AI 服务；没有实现新的跨文件 AI agent。

## AI agent 的方向

保留独立本地核心，在可选“深入理解”中让模型读取授权范围内的引用文件、追踪相关实现，并给出带证据的解释。执行/修改与解释分开授权，项目中的文本不能扩张权限，模型调用和读取范围应有限制。仅更名或多轮调用模型不会自动提高准确度。本版本只整理可复用的解释接口，没有引入未使用的 agent 框架。
