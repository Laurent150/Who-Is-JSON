(function(root,factory){if(typeof module==="object"&&module.exports)module.exports=factory;else root.WhoJavascriptCards=factory;})(this,function(card){
 card('js.destructure','JavaScript','拆开取值 · 解构赋值','按位置把一组内容分别交给几个名字，不必一项一项写取值语句。','方括号和赋值的写法是语法；接收值的名字由作者自己起。','const [price, stock] = [20, 5];','price 得到 20，stock 得到 5。','顺序很重要。右边必须能按顺序取值；普通对象不能直接这样拆。','[price,stock]',[20,5]);
 card('js.object-destructure','JavaScript','按名称取值 · 对象解构','从一份带字段名的数据里，取出你需要的几项。','属性名要对应数据里的字段；冒号后可以给取出的值另起名字。','const {name: userName} = {name: "小林", age: 18};','userName 得到“小林”。','它按字段名取值，不按字段排列顺序。','userName','小林');
 card('js.hypot','JavaScript','从两条边算直线距离','把输入的数分别平方，再把它们相加，最后开平方。两项输入可以表示直角三角形的两条直角边。','Math.hypot 是 JavaScript 自带方法；使用这个方法时保留名称。','const distance = Math.hypot(3, 4);','distance 得到 5：横向走 3、纵向走 4，对应直线距离是 5。','需要先弄清传入的数代表什么。给它两个坐标本身，并不等于算出了任意两点的距离。','distance',5);
 card('js.variable','JavaScript','给数据起名字 · 变量','把一个值保存在名字下面，后面的代码就可以用这个名字找到它。','const 和 let 是固定写法；后面的变量名由作者起。const 不允许把名字重新指向另一个值。','let score = 10;\nscore = score + 5;','score 变成 15。','const 保存的对象内部仍可能改变；不能把 const 理解成所有内容都不变。','score',15);
 card('js.condition','JavaScript','满足条件才做 · if','先检查一个条件，再决定要不要执行里面的步骤；有 else 时，不满足就走另一条路。','if、else 是固定写法；条件里用到的数据名通常由作者起。','const age = 16;\nlet ticket;\nif (age < 18) {\n  ticket = "学生票";\n} else {\n  ticket = "成人票";\n}','ticket 得到“学生票”。','没有 else 不代表出错，只是不满足时跳过这一段。','ticket','学生票');
 card('js.compare','JavaScript','比较之后得到“是”或“否”','比较会产生 true 或 false，常用来决定接下来走哪个步骤。','=== 要求类型和值都相同；!== 表示不完全相同。它们都是固定运算符。','const same = 3 === "3";\nconst different = 3 !== "3";','same 是 false，different 是 true：数字 3 和文字“3”不是同一种值。','不要把赋值的 = 当成比较的 ===。','[same,different]',[false,true]);
 card('js.choice','JavaScript','两种结果选一种 · 条件表达式','条件成立取问号后面的结果，不成立取冒号后面的结果。','? 和 : 是固定写法，结果存到什么名字由作者决定。','const count = 2;\nconst message = count > 0 ? "有库存" : "已售罄";','message 得到“有库存”。','多层嵌套会难读；学习时先把它拆成逐个 if 判断。','message','有库存');
 card('js.logic','JavaScript','组合多个条件','&& 要求前面成立才继续看后面；|| 在前面不成立时才继续看后面。','&&、||、! 是语言的固定运算符。! 会把真假结果反过来。','const canEnter = true && false;\nconst hasOption = false || true;','canEnter 是 false，hasOption 是 true。','&& 和 || 不一定返回布尔值，也可能返回参与判断的某个原始值。','[canEnter,hasOption]',[false,true]);
 card('js.return','JavaScript','把结果交回去 · return','结束这一次函数处理，把后面的值交给调用它的地方。','return 是固定写法；返回什么值由这段功能决定。','function double(number) {\n  return number * 2;\n}\nconst answer = double(6);','answer 得到 12。','return 之后，同一路径上的剩余步骤不会继续；它不是把内容显示到屏幕上。','answer',12);
 card('js.function','JavaScript','把步骤做成小工具 · 函数','把一组步骤放在一起，需要时交给它输入，让它完成处理。','function 是一种定义写法，箭头 => 是另一种；参数名和普通函数名由作者起。','function addTax(price) {\n  return price * 1.1;\n}\nconst total = addTax(100);','total 约为 110。这里只展示用法，数字计算可能有小数精度差异。','定义函数通常不会执行函数体；要调用它才会进入。','Math.round(total)',110);
 card('js.loop','JavaScript','把同一件事重复做 · 循环','让一组步骤重复作用于多项内容，或重复到条件不再成立。','for、while 是固定写法；每轮处理的数据名由作者起。','let total = 0;\nfor (const price of [10, 20]) {\n  total += price;\n}','总数先设为 0；依次取出 10、20，每取一项就加到总数里。第一次变为 10，第二次变为 30。','要有结束方式；条件永远成立可能让循环不停运行。','total',30);
 card('js.map','JavaScript','每项变成新内容 · map','按同一规则转换每项，把得到的结果收集为新列表。','map 是标准列表方法；回调里的名字由作者起。同名自定义方法需要另看实现。','const doubled = [2, 4, 6].map(n => n * 2);','doubled 得到 [4, 8, 12]。','回调需要给出结果；使用花括号时别漏掉 return。','doubled',[4,8,12]);
 card('js.filter','JavaScript','只留下符合条件的项 · filter','逐项检查，只把通过检查的内容放进新列表。','filter 是标准列表方法；条件由作者决定。同名自定义方法可能不同。','const affordable = [10, 35, 20].filter(price => price <= 20);','affordable 得到 [10, 20]。','filter 负责筛选，不负责把每项转换成另一种值。','affordable',[10,20]);
 card('js.join','JavaScript','把多项连成文字 · join','在各项之间放入指定分隔文字，得到一整段文字。','join 是标准列表方法；引号里的分隔符由作者选择。','const label = ["苹果", "梨", "桃"].join("、");','label 得到“苹果、梨、桃”。','分隔符会放在项与项之间，不会自动加在开头或结尾。','label','苹果、梨、桃');
 card('js.trim','JavaScript','清掉文字两头的空白 · trim','去掉开头和结尾的空格、换行等空白，得到整理后的文字。','trim 是标准文字方法；点号前面的数据名由作者起。','const name = "  小 林  ".trim();','name 得到“小 林”，中间的空格仍在。','trim 不会去掉文字中间的空格，也不会自动修改原来的变量。','name','小 林');
 card('js.foreach','JavaScript','为每项做一件事 · forEach','依次把每项交给一组步骤处理。它自身不会收集回调返回的新结果。','forEach 是标准列表方法；回调参数名由作者起。','let total = 0;\n[10, 20].forEach(price => { total += price; });','总数先设为 0；依次取出 10、20，每取一项就加到总数里。第一次变为 10，第二次变为 30。','回调里的 return 只结束当前这次回调，不是退出整个外层函数，也不是 break。','total',30);
 card('js.template','JavaScript','把数据放进文字 · 模板字符串','先写一段文字，在指定位置填入值，得到完整句子。','反引号和 ${…} 是固定写法；大括号里写需要填入的值。','const count = 3;\nconst message = `购物车有 ${count} 件商品`;','message 得到“购物车有 3 件商品”。','普通单引号或双引号不会把 ${…} 当成填值位置。','message','购物车有 3 件商品');
 card("js.binding","JavaScript","固定名字的指向 · const / let","const 声明后不能把这个名字重新指向其他值；let 可以重新赋值。","const、let 是固定语法；后面的名称由作者起。","const basket = {count: 1};\nbasket.count = 2;","篮子里面的 count 可以变成 2。","const 固定的是名字的指向，不保证对象内部不变。","basket.count",2);
 card("js.arrow","JavaScript","用箭头定义一个小工具 · =>","箭头左侧是输入，右侧是处理方式；保存后可以用名字和括号调用。","=> 是固定写法；输入名和保存它的名字由作者起。","const square = value => value * value;\nconst answer = square(5);","answer 得到 25。","箭头函数和普通函数的 this、构造调用行为不同，不能在所有场景互换。","answer",25);
 card("js.implicit-return","JavaScript","省略 return 的箭头写法","箭头右边直接写表达式时，它算出的值就是这次调用的结果。","没有花括号的表达式体会自动返回；有花括号时需显式 return。","const double = n => n * 2;\nconst answer = double(4);","answer 得到 8。","n => { n * 2; } 没写 return，会得到 undefined；返回对象需写成 n => ({value:n})。","answer",8);
 card("js.typeof","JavaScript","询问值的种类 · typeof","得到表示类型的文字，例如 string 表示文字、number 表示数字。","typeof 是固定运算符；它返回的 string 是语言约定的类型名称。","const kind = typeof \"你好\";\nconst isText = kind === \"string\";","kind 是 \"string\"，isText 是 true。","typeof null 是 \"object\"；它不是判断所有具体类型的万能工具。","[kind,isText]",["string", true]);
 card("js.empty-string","JavaScript","没有任何字符的文字 · 空字符串","两个引号紧挨着，表示长度为 0 的文字。它是文字值，不代表没有提供值。","空引号是文字值的写法，里面没有需要命名的内容。","const empty = \"\";\nconst space = \" \";\nconst lengths = [empty.length, space.length];","lengths 是 [0, 1]：一个空格也算字符。","\"\" 与空格、null、undefined 都不同；这里用它作为默认文字结果。","lengths",[0, 1]);
 card("js.string","JavaScript","引号里面是文字 · 字符串","引号内的内容作为文字使用，不会被当成变量名去找值。","引号是语法；文字内容通常由作者写，协议、字段或类型名需符合约定。","const color = \"blue\";\nconst word = \"color\";","color 保存 blue；word 保存文字 color。","\"string\" 在 typeof 比较里是规定的类型名，不能随意换词。","[color,word]",["blue", "color"]);
 card("js.number","JavaScript","直接写出数字","数字可以参与运算和比较，不等于引号里的数字文字。","数字是值；保存数字的名称由作者起。","const total = 3 + 4;","total 得到 7。","数字计算有精度范围，不能据此保证任意金额或大整数精确。","total",7);
 card("js.array","JavaScript","按顺序装在一起 · 数组","把多项值保存在一份有顺序的列表里，位置从 0 开始。","[] 是固定写法；列表名由作者起。","const fruits = [\"苹果\", \"梨\"];\nconst first = fruits[0];","first 得到苹果。","位置从 0 开始；超出范围通常得到 undefined。","first","苹果");
 card("js.object","JavaScript","用名称整理数据 · 对象","给每项内容加上字段名，之后按字段取值。","花括号和冒号属于语法；字段名需和使用方一致。","const user = {name: \"小林\", age: 18};\nconst age = user.age;","age 得到 18。","对象不是按位置取值的列表；复制引用也不是复制全部内容。","age",18);
 card("js.arithmetic","JavaScript","计算数值或连接文字","运算符把值组合成新结果；加号还可以连接文字。","+、-、*、/ 等是固定运算符。","const sum = 3 + 4;\nconst label = \"数量：\" + 3;","sum 是 7；label 是数量：3。","先确认值的类型；\"3\" + 4 得到文字 \"34\"。","[sum,label]",[7, "数量：3"]);
 card('js.json-read','JavaScript','把 JSON 文字读成数据','将 JSON 文字转换为程序可以读取的数据。','JSON 是自带对象，parse 是它的方法名；data 是作者起的名字。','const data = JSON.parse(\'{"count":2}\');\nconst answer = data.count;','answer 得到数字 2。','格式正确不代表字段齐全；JSON 也可以是列表、数字或 null。','answer',2);
 card('js.json-write','JavaScript','把数据写成 JSON 文字','把可转换的数据写成 JSON 文字，方便保存或传递。','JSON 是自带对象，stringify 是它提供的方法。','const text = JSON.stringify({count:2});','text 得到 {"count":2} 这段文字。','循环引用会报错；函数等内容可能被忽略，不是完整复制任意对象。','text','{"count":2}');
});
