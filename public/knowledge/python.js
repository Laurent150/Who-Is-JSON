(function(root,factory){if(typeof module==="object"&&module.exports)module.exports=factory;else root.WhoPythonCards=factory;})(this,function(card){
 card('py.path','Python','Path 中的 / · 拼接路径','对于 pathlib 的路径对象，/ 用来接上路径的一部分；这里不是数字相除。','Path 来自 Python 标准库 pathlib；路径内容由作者或输入提供。','from pathlib import Path\nfile = Path("docs") / "notes.txt"\ntext = file.as_posix()','第二行拼出 docs 下的 notes.txt；text 是 docs/notes.txt。这里只准备路径，没有创建文件。','若右侧是绝对路径，可能替换前面的路径；类型提示不保证调用时一定收到路径对象。','text','docs/notes.txt');
 card('py.generator','Python','需要时才提供下一项 · 生成器','带 yield 的函数在调用后先得到生成器。开始取值才执行内部步骤；yield 提供一项并暂停，下次取值时继续。','yield、yield from 是语法；函数和变量名由作者起。','def numbers():\n    yield 2\n    yield 4\nstream = numbers()\nfirst = next(stream)\nsecond = next(stream)','第 4 行得到生成器；第 5 行取到 2，第 6 行接着取到 4。','取完后继续 next 会报告 StopIteration；异步生成器用 async for 等方式读取。','[first,second]',[2,4]);
 card('py.set','Python','只保留不同的项 · 集合','集合保存不重复的项，可以检查某项是否在里面；不提供列表那样的位置顺序。','set 是内置类型；非空集合可以写成 {1, 2}，空集合用 set()。','items = {2, 2, 3}\ncount = len(items)\nexists = 3 in items','两个 2 合成同一项，所以 count 是 2，exists 是 True。','不能用 items[0] 取第一项；{} 是空字典。集合项必须可哈希，例如整数、文字。','[count,exists]',[2,true]);
 card('py.collection-comprehension','Python','逐项计算，组成集合或字典','在花括号内写 for，可以按输入逐项计算，形成集合或键值对应表。','for、in 是语法；有 key: value 的形式组成字典，只有一个值的形式组成集合。','squares = {n: n * n for n in [2, 3]}\nunique = {n % 2 for n in [1, 2, 3]}','squares 把 2 对应到 4、3 对应到 9；unique 只保留余数 0 和 1。','重复的字典键保留后面的值；集合不保留重复项，不能假设它的显示顺序。','[squares[2],squares[3],sorted(unique)]',[4,9,[0,1]]);
 card('py.conditional-value','Python','按条件选一个值 · 简写 if','先检查 if 后面的条件，成立时用左边的值，否则用 else 后面的值。','if、else 是固定写法；这是一段产生值的表达式。','age = 12\nticket = "儿童" if age < 14 else "成人"','12 小于 14，所以只选择“儿童”，保存到 ticket。','两边的表达式不会都计算；不要把这种写法当成两个独立步骤。','ticket','儿童');
 card('py.lambda','Python','写一个很短的函数 · lambda','lambda 后写参数，冒号后写调用时要计算并交回的结果。','lambda 是关键字；参数名由作者起。','double = lambda n: n * 2\nanswer = double(5)','第 1 行准备函数；第 2 行传入 5，算出并交回 10。','定义 lambda 时不会提前计算函数体；它也会受到外部名字后续变化的影响。','answer',10);
 card('py.assert','Python','检查这里是否符合预期 · assert','条件不成立时报告 AssertionError，常用来检查程序内部假设。','assert 是关键字；可在逗号后写错误说明。','number = 3\nassert number > 0, "必须为正数"\nanswer = number * 2','3 大于 0，检查通过，继续计算得到 6。','Python 优化模式可能移除 assert；不能把它当作必须执行的用户输入或权限检查。','answer',6);
 card('py.match','Python','按形状或内容选分支 · match','从上往下尝试 case；模式匹配且附加条件成立时，执行该分支。','match、case 在这里是语法；case 中的普通名字可能接收值，并非比较常量。','value = ["ok", 7]\nmatch value:\n    case ["ok", number]:\n        answer = number\n    case _:\n        answer = 0','第一种模式匹配成功，把第二项 7 交给 number，所以 answer 是 7。','需要 Python 3.10+；_ 是通配模式。只执行第一个符合条件的分支。','answer',7);
 const callLesson=card('py.call','Python','把数据交给一组步骤 · 调用','名字后面加括号，就是调用它。先求出括号里的值，再交给接收方。','函数名可以由作者定义或从其他文件导入；不能只凭首字母大写就判定是类。','def double(number):\n    return number * 2\nanswer = double(3)','第 3 行把 3 交给 number；第 2 行算出 6 并交回，answer 保存 6。','调用类通常得到对象；调用函数可能得到其他类型的结果。具体行为要看定义。','answer',6);
 const keywordLesson=card('py.keyword','Python','按名字传入数据 · 参数名=值','左边写对方接收用的名字，右边写这次要提供的值。','等号左边的名字要对应接收方的参数；右边变量的名字由当前代码决定。','def label(name, count):\n    return name + "：" + str(count)\namount = 2\ntext = label(count=amount, name="苹果")','第 3 行 amount 是 2。第 4 行把 2 交给 count，把“苹果”交给 name；最后 text 是“苹果：2”。','这里的 = 是传参数，不是在调用处重新给 count 或 name 赋值；参数名写错可能报错。','text','苹果：2');
 card('py.attribute','Python','点号 · 找到它保存的数据或功能','a.b 表示从 a 上访问名为 b 的属性；属性可以是数据，也可以是可调用的功能。','点号是固定写法；右侧名称由对应对象、模块或类定义。','class Options:\n    def __init__(self):\n        self.limit = 3\noptions = Options()\namount = options.limit','第 4 行准备一个 Options 对象，它保存 limit=3；第 5 行读出这个值，所以 amount 是 3。','点号不保证该名称一定存在；也不能仅凭名称知道真实值。自定义属性读取还可能执行额外代码。','amount',3);
 const tupleLesson=card('py.tuple','Python','把几项按顺序一起交回 · 元组','元组是一组有顺序的值。return a, b 会将两项装成一个元组，一次交回。','tuple 是内置类型名；常见非空元组用逗号组成，括号主要用于分组。','def prices():\n    apple = 3\n    pear = 5\n    return apple, pear\nresult = prices()\nfirst, second = result','第 4 行将 3、5 按顺序一起交回；result 是 (3, 5)。第 6 行分别接收两项，first 是 3，second 是 5。','tuple[str, int] 是类型提示，不是实际结果；元组不能直接替换某一项，项内部若为列表等仍可能被修改。','[list(result),first,second]',[[3,5],3,5]);
 card('py.self','Python','self · 当前正在使用的这个对象','对象可以保存自己的数据。方法里的 self 指向这次操作的对象，self.name 就是访问它的 name。','self 是习惯用名，不是关键字；__init__ 是 Python 为对象开始时的准备工作约定的名称。','class Ticket:\n    def __init__(self, name):\n        self.name = name\nfirst = Ticket("小林")\nsecond = Ticket("小周")','第 4 行准备 first，把“小林”存到它的 name；第 5 行准备另一个对象 second，存入“小周”。两个对象分别保留自己的名字。','self.name = name 左边是对象保存的位置，右边是这次收到的参数；不能把两边理解成同一个位置。','[first.name,second.name]',['小林','小周']);
 const jsonLesson=card('py.json-read','Python','把 JSON 文字读成数据','把 JSON 文字转换为 Python 数据，之后才能按其形状读取内容。','json 是标准库，loads 是它提供的方法名；text、data 由作者起。','import json\ntext = \'{"count": 2}\'\ndata = json.loads(text)\nanswer = data["count"]','answer 得到数字 2。','格式正确不代表包含 count；JSON 也可以表示列表、数字或空值，不一定是字典。','answer',2);
 card('py.json-write','Python','把数据写成 JSON 文字','将程序中的数据转换为便于保存和交换的 JSON 文字。','json 是标准库，dumps 是它提供的名称。','import json\ntext = json.dumps({"count": 2})','text 得到 JSON 文字 {"count": 2}。','JSON 不能保存任意 Python 对象；可选参数可能改变转换行为。','text','{"count": 2}');
 card('py.regex-replace','Python','按规则替换 · re.sub','按规则找到文字中的匹配部分，再用新内容替换。','re 是标准库，sub 是它提供的功能名；规则文字由作者填写。','import re\ntext = re.sub(r"[0-9]+", "#", "订单123")','text 得到订单#。','正则是一种匹配规则；错误的规则可能修改不该修改的内容。','text','订单#');
 card('py.text-strip','Python','去掉两端空白 · strip','默认去掉文字首尾的空白，保留中间空格，生成新文字。','strip 是字符串提供的方法；text 是作者起的名字。','text = "  hello world  ".strip()','text 得到 hello world。','带参数时按一组字符移除，不是删除完整前缀；原字符串不被原地修改。','text','hello world');
 card('py.text-replace','Python','替换指定文字 · replace','把旧文字替换为新文字，得到新字符串。','replace 是字符串方法；要替换的内容由参数提供。','text = "红苹果和红花".replace("红", "绿", 1)','text 得到绿苹果和红花。','第三个参数限制替换次数；这是普通文字匹配，不是正则匹配。','text','绿苹果和红花');
 const inherited=card('py.super','Python','复用继承来的初始化 · super','沿继承查找顺序调用后续实现，让已有部分先完成准备。','super 是内置工具，__init__ 是约定方法名；类名和字段名由作者起。','class Base:\n    def __init__(self, name):\n        self.name = name\nclass User(Base):\n    def __init__(self, name):\n        super().__init__(name)\n        self.active = True\nuser = User("小林")','user.name 是“小林”，user.active 是 True。两项数据都保存在同一个 user 对象上。','多重继承时按方法查找顺序，不一定是直接父类；具体行为需要查看相应实现。','[user.name,user.active]',['小林',true]);
 inherited.prerequisites=[{name:'继承是什么？',text:'User(Base) 表示 User 可以复用 Base 中定义的功能。在这个例子里，Base 负责保存名字，User 再增加是否启用的记录。'},{name:'self 是谁？',text:'self 代表当前正在处理的对象。在这两个初始化方法里，self 都指向同一个新创建的 User 对象。'}];
 inherited.walkthrough=['先定义 Base 和 User 两个类。这些定义描述怎么准备对象，此时还没有创建 user。','执行 user = User("小林")，开始准备一个 User 对象。User 的初始化方法收到 name="小林"。','super().__init__(name) 在这个例子中找到 Base 的初始化方法；self.name = name 把“小林”保存在同一个对象上。','回到 User 的初始化方法，self.active = True 再保存启用状态。初始化结束后，user 指向准备好的对象。'];
 inherited.transfer='对应当前源码：name 对应传给初始化方法的数据；Base 对应继承来源。当前源码里的实际初始化行为仍要查看继承来源的定义，不能套用这个例子的字段。';
 inherited.exercise='如果只去掉例子中的 super().__init__(name)，user.active 仍会设为 True，但这份例子没有其他步骤设置 name；读取 user.name 就会报属性不存在的错误。';
 card('py.retry','Python','失败后再试，但限制次数','在指定错误发生后更新计数，再尝试同一组操作。','while、try、except 是语法；attempt 等计数名字由作者起。','attempt = 0\nwhile attempt < 3:\n    try:\n        value = int(["错误", "7"][attempt])\n        break\n    except ValueError:\n        attempt += 1','先把“错误”转成整数，失败后计数加 1，改试下一项“7”；转成数字 7 后，break 结束循环。','次数上限包含第一次；未被捕获的错误会直接向外报告，重试不保证成功。','[attempt,value]',[1,7]);
 card('py.length','Python','数一数有多少 · len','数文字的字符数，或列表等容器的项数。','len 是 Python 内置名称；输入名字由作者起。','size = len("你好")','size 得到 2。','字符数不等于文件字节数，也不是 AI 的精确 token 数。','size',2);
 card('py.extrema','Python','选最大或最小 · max / min','从多个值或一组可比较的值中，选出最大或最小的一项。','max、min 是内置函数；未被同名定义覆盖时可直接使用。','floor = max(1, 10 // 3)\nsmall = min([8, 2, 5])','floor 是 3，small 是 2。','max(1, x) 可以给结果设下限；空序列没有默认值时会报错。','[floor,small]',[3,2]);
 card('py.convert','Python','在文字和数字之间转换','str 转成文字；int 尝试转成整数；float 尝试转成小数。','这些名称是内置工具，不是作者起的名字。','number = int("12")\ntext = str(number)','number 是数字 12；text 是文字 12。','int("苹果") 会报错；int(3.9) 截去小数部分，与 // 的负数取整规则不同。','[number,text]',[12,'12']);
 card('py.sum','Python','把一组数相加 · sum','累加可逐项读取的一组数，得到总数。','sum 是内置函数，输入的名字由作者起。','total = sum([3, 5, 2])','total 是 10。','不能用 sum 直接连接文字。','total',10);
 card('py.range','Python','提供一段整数 · range','按起点、终点和步长产生整数，常用于控制重复次数。','range 是内置工具；for、in 是语法。','positions = list(range(4, -1, -2))','positions 是 [4, 2, 0]。','不包含终点；负步长向回数，步长不能为 0。','positions',[4,2,0]);
 card('py.await','Python','等待任务，允许其他任务继续 · await','在异步功能中等待结果，等待期间让事件循环有机会处理其他任务。','async、await 是固定写法；asyncio 是标准库。','import asyncio\nasync def double():\n    await asyncio.sleep(0)\n    return 6 * 2\nanswer = asyncio.run(double())','answer 是 12。','调用 async 函数只得到待运行的协程；await 本身不保证并行，也不会自动把普通阻塞操作变成异步。','answer',12);
 card('py.tasks','Python','安排任务并等待 · asyncio','create_task 安排协程运行；wait 等待一组任务达到条件，返回已完成和仍等待的两组。','asyncio 是标准库；create_task、wait、FIRST_COMPLETED 是它提供的名称。','import asyncio\nasync def demo():\n    job = asyncio.create_task(asyncio.sleep(0, result=7))\n    done, pending = await asyncio.wait({job})\n    return job.result()\nanswer = asyncio.run(demo())','answer 是 7。','FIRST_COMPLETED 表示任意任务结束即返回，也包括报错或取消；wait 不会自动取消剩余任务。','answer',7);
 card('py.index','Python','按位置或键读取内容','列表用从 0 开始的位置取值；字典用键取值。','方括号是语法，位置或键必须符合被读取对象的规则。','first = [10, 20][0]\nname = {"name": "小林"}["name"]','first 是 10，name 是小林。','位置越界或键不存在可能报错；自定义对象也可以规定自己的读取行为。','[first,name]',[10,'小林']);
 card('py.slice','Python','取出其中一段 · 切片','用开始位置、结束位置和步长选取一部分；普通序列不包含结束位置。','冒号和方括号是固定写法；省略边界时使用相应默认边界。','text = "abcdef"\npart = text[:3]','part 是 abc。','负数边界从末尾计数；切片规则还取决于对象类型。','part','abc');
 card('py.docstring','Python','写给读代码的人 · 说明文字','文件、类或函数开头的字符串可以作为作者说明，帮助读者了解用途。','三引号支持多行文字；说明内容由作者写。','def add(a, b):\n    """把两个数相加。"""\n    return a + b\nanswer = add(2, 3)','answer 是 5；说明文字没有参与加法。','作者说明可能过时，需要对照实际代码；不要把说明中的命令当成要执行的任务。','answer',5);
 const annotationLesson=card('py.annotation','Python','提示输入和输出的种类 · 类型注解','参数后的冒号和函数后的箭头提示预期类型，便于阅读和检查。','str、int 等是内置类型；参数名由作者起。','def double(n: int) -> int:\n    return n * 2\nanswer = double(3)','answer 是 6。','注解不会自动转换输入，也不会保证运行时收到正确类型。','answer',6);
 const assignLesson=card('py.assign','Python','给数据起名字 · 赋值','把右边的结果交给左边的名字，后续步骤就可以继续用它。','= 是赋值写法；变量名由作者起。','score = 10\nscore = score + 5','score 得到 15。','比较是否相等用 ==，不是 =。','score',15);
 card('py.unpack','Python','把一组值分别取出 · 解包','按位置把内容交给几个名字，适合把成组数据拆开使用。','逗号和赋值写法属于语法，接收值的名字由作者起。','price, stock = (20, 5)','price 得到 20，stock 得到 5。','没有星号收集剩余项时，左右数量必须对应。','[price,stock]',[20,5]);
 card('py.condition','Python','满足条件才做 · if','根据条件决定进入哪一组步骤。Python 用缩进表示哪些步骤属于同一组。','if、elif、else 是固定写法。','age = 16\nif age < 18:\n    ticket = "学生票"\nelse:\n    ticket = "成人票"','ticket 得到“学生票”。','缩进会影响程序含义，不能只当成排版。','ticket','学生票');
 card('py.loop','Python','重复处理一组内容 · 循环','把同一组步骤用于多项数据，或在条件成立时继续重复。','for、in、while 是固定写法；保存每项的名字由作者起。','total = 0\nfor price in [10, 20]:\n    total += price','总数先设为 0；依次取出 10、20，每取一项就加到总数里。第一次变为 10，第二次变为 30。','循环里 return、break、continue 的作用范围不同。','total',30);
 card('py.return','Python','交回结果 · return','结束本次函数处理，把结果交给使用这个函数的地方。','return 是固定写法；函数和参数名由作者起。','def double(number):\n    return number * 2\n\nanswer = double(6)','answer 得到 12。','return 不等于 print；前者交回结果，后者显示内容。','answer',12);
 const functionLesson=card('py.function','Python','准备可重复使用的步骤 · 函数','先定义一组步骤，之后用函数名和括号调用它。','def 是固定写法，函数名和参数名由作者起。','def greet(name):\n    return "你好，" + name\n\nmessage = greet("小林")','message 得到“你好，小林”。','调用时提供的内容会交给参数；只定义不会执行函数体。','message','你好，小林');
 card("py.compare","Python","比较得到真假","比较两个值，得到 True 或 False，供后面的判断使用。","==、!=、< 等属于语法；True、False 是固定布尔值。","allowed = 18 >= 16","allowed 是 True。","赋值用 =；is 判断是否同一对象，通常不用于判断文字是否相同。","allowed",true);
 card("py.typecheck","Python","先确认是哪种值 · isinstance","询问一个值是否属于指定类型，也接受该类型的子类。","isinstance 是 Python 内置函数；str、int 等是内置类型，未被同名变量覆盖时可直接使用。","is_text = isinstance(\"你好\", str)","is_text 是 True。","第二项是类型 str，不是文字 \"str\"；名字也可能被用户代码覆盖。","is_text",true);
 card("py.import","Python","把其他文件的工具引进来","导入后可以通过模块名或导入的名字使用其中的功能。","import、from、as 是语法；模块路径必须存在，别名由作者起。","import math\nlength = math.hypot(3, 4)","length 得到 5。","只看到导入语句，不能确认第三方模块具体实现；导入也可能运行模块顶层代码。","length",5);
 card("py.comprehension","Python","用简短写法生成一组结果","按顺序取出每项，筛选并计算后放入新列表。","for、in、if 是固定写法；每项的名字由作者起。","doubles = [n * 2 for n in [1, 2, 3] if n > 1]","doubles 得到 [4, 6]。","列表推导会生成新列表；圆括号形式的生成器并不立即生成全部结果。","doubles",[4, 6]);
 card("py.string","Python","文字和空文字","引号中放文字，空引号表示长度为 0 的文字。","引号是语法，文字内容由作者或使用约定决定。","empty = \"\"\nspace = \" \"\nlengths = [len(empty), len(space)]","lengths 得到 [0, 1]。","空字符串不等于 None；文字中的空格也是内容。","lengths",[0, 1]);
 card("py.list","Python","有顺序的一组值 · 列表","用一个列表保存多项内容，可以按位置读取和修改。","[] 是固定写法；列表名由作者起。","items = [10, 20]\nfirst = items[0]","first 得到 10。","第一个位置是 0；越界读取会抛出 IndexError。","first",10);
 card("py.dict","Python","用键找到对应内容 · 字典","为每项值指定一个键，通过键查找值。","{} 和冒号是写法；键要与读取处一致。","user = {\"name\": \"小林\", \"age\": 18}\nage = user[\"age\"]","age 得到 18。","直接读取不存在的键会抛出 KeyError；get 可以提供默认值。","age",18);
 card("py.class","Python","把数据和操作放在一起 · 类","类描述一类对象保存什么数据、能做什么；实例是按这个描述创建的具体对象。","class、def 是语法；self 是常用参数约定，类名和方法名由作者起。","class Counter:\n    def __init__(self):\n        self.value = 0\n    def add(self):\n        self.value += 1\nc = Counter()\nc.add()","c.value 得到 1。","self 指向这次操作的实例；各实例可以有不同的数据。","c.value",1);
 card("py.exception","Python","为出错准备另一条路 · try","尝试操作，出现指定错误时进入 except 中的处理。","try、except 是固定写法；异常类型需符合实际错误。","try:\n    value = int(\"苹果\")\nexcept ValueError:\n    value = 0","转换失败后，value 得到 0。","捕获错误不等于解决错误；不要无差别隐藏所有异常。","value",0);
 card("py.with","Python","用完自动收尾 · with","使用支持上下文管理的资源，离开这组步骤时执行收尾操作。","with、as 是语法；资源名由作者起。","from io import StringIO\nwith StringIO(\"你好\") as stream:\n    text = stream.read()\nclosed = stream.closed","text 是你好，离开后 closed 是 True。","with 调用上下文管理协议，不是所有对象都能用；收尾行为由对象实现决定。","[text,closed]",["你好", true]);
 card("py.logic","Python","把条件组合起来","and 在前项成立时看后项，or 在前项不成立时看后项；not 取反。","and、or、not 是固定写法。","ready = True and not False","ready 是 True。","and、or 可能交回某个原始值，不一定是 True 或 False。","ready",true);
 card("py.arithmetic","Python","用数值计算新结果","把数按运算规则组合起来，得到下一步需要的数值。","+、-、*、/、//、% 是固定运算符。","half = 7 // 2\nrest = 7 % 2","half 是 3，rest 是 1。","// 是向下取整；负数时不等于简单截掉小数部分。","[half,rest]",[3, 1]);
 card("py.format","Python","把数据填进文字 · f-string","在带 f 的文字里，用花括号插入当前值。","f 前缀和花括号是语法；插入的变量名由作者起。","count = 3\nmessage = f\"共有 {count} 件\"","message 是共有 3 件。","普通字符串不会自动处理花括号里的变量。","message","共有 3 件");
 functionLesson.walkthrough=["先定义 greet：它需要接收一个 name，此时还没有生成问候语。", "执行 greet(\"小林\") 时，name 收到“小林”。", "return 把拼好的“你好，小林”交回调用处，保存到 message。"];
 annotationLesson.walkthrough=["n: int 提示希望收到整数；-> int 提示希望交回整数，它们都不是转换指令。", "调用 double(3)，n 收到数字 3；n * 2 算出 6。", "return 把 6 交回，保存到 answer。"];
 assignLesson.walkthrough=["score = 10 先让 score 对应数字 10。", "下一行先算右边：旧 score 的 10 加 5 得到 15。", "再把 15 交给左边的 score，所以它现在对应 15。"];
 jsonLesson.walkthrough=["text 保存的是带有 JSON 格式的文字，还不是可按字段读取的字典。", "json.loads(text) 读取这段文字，得到包含 count 的字典，并保存到 data。", "data[\"count\"] 用字段名取出数字 2，再保存到 answer。"];
});
