# AI 功能实测记录

2026-09-15，Windows / Edge，DeepSeek 官方接口、deepseek-flash。使用明确授权的测试密钥，报告不包含密钥；图片为自编教学内容，公开样本只作静态分析。

## 复现与修复

原版本对 435 行 `tests/corpus/binary-search.py` 发起完整 AI 分析，约 90.2 秒后返回模型响应超时。请求未限制生成长度，DeepSeek 使用默认思考模式，页面全程只有等待按钮。

现在先返回本地流程，再请求有长度限制的 AI 用途、示例与术语说明。DeepSeek 使用非思考模式，取消或失败时保留本地结果，错误按网络权限、DNS、证书、超时及 HTTP 状态分别说明。AI 不替换本地解析范围与流程。

真实质量复查发现模型偶尔把选中行错认成邻行。修复为服务器提取选中原文，与完整上下文分开传入。另收紧提示词，避免给 Python 中间位置计算添加没有依据的性能动机。回答仍需核对；测试通过不证明任意源码都能准确解释。

## 最近一轮完整调用结果

| 项目 | 结果 | 时间 |
| --- | --- | --- |
| 显示本地流程 | 成功，AI 等待时即可阅读 | 0.386 秒 |
| 435 行源码 AI 总览 | 成功，保留本地功能与流程 | 18.687 秒（从开始分析计） |
| 选中语句 AI 解释 | 成功，附小数值推演 | 2.377 秒 |
| 针对插入位置的追问 | 成功，正确解释位置 2 和从 0 编号 | 1.376 秒 |
| AI 图片识别 | 成功，识别两行自编 Python | 0.862 秒 |
| 可选讲解稿 | 包含 AI 用途说明 | 页面检查通过 |

这些时间是该次网络与模型条件下的观测，不是性能保证。完整真实调用进行了多轮复查；收紧措辞后的额外单句请求也返回 200，正确说明 `lo=4, hi=10` 时 `mid=7`，并说明下一步比较中间元素。

## 回归范围

- 202 项自动测试、19 份公开样本检查通过。
- 新旧阅读视图、源码与流程联动、收藏保留及手机布局通过浏览器检查。
- 模拟等待、取消、鉴权失败和输入变化：本地结果保留，过期请求不覆盖新输入。
- 传输层覆盖响应体超时、截断、无效 JSON 与错误分类。
- 最终提示词改动后，额外复跑 5 项 AI 客户端与 HTTP API 测试及浏览器状态检查，均通过。

可复现脚本及运行要求见 [逐行读懂说明](LINE_READING.md)。真实调用为显式开启的测试，不在默认测试中自动付费调用。尚未进行真实初学者理解实验、安装包更新验收或全部模型服务的兼容性测试。
